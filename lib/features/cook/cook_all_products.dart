import 'package:flutter/material.dart';

class CookAllProducts extends StatelessWidget {
  const CookAllProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
