import 'package:flutter/material.dart';

class CookProductDetail extends StatelessWidget {
  const CookProductDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
